[project]
name = "poc"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "anyio",
]

[tool.uv.sources]
httpx = { git = "https://github.com/encode/httpx", tag = "0.27.2" }
