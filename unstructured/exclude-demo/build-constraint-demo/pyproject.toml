[project]
name = "build-constraint-demo"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["httpx"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
build-constraint-dependencies = ["setuptools>=70"]
