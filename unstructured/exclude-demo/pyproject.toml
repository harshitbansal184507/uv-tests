[project]
name = "exclude-demo"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["httpx"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
exclude-dependencies = ["exceptiongroup"]