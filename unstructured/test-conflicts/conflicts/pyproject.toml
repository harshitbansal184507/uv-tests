[project]
name = "conflicts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
torch = ["torch==2.0.0", "torchvision==0.15.0"]
tensorflow = ["tensorflow==2.12.0", "keras==2.12.0"]

[tool.uv]
conflicts = [
    [
        { extra = "torch" },
        { extra = "tensorflow" },
    ]
]
