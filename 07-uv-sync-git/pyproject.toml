[project]
name = "sample-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask",
]

[tool.uv.sources]
flask = { git = "https://github.com/pallets/flask", rev = "3.1.1" }